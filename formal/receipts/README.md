# Native kernel receipts

Receipts in this campaign are generated only by GitHub Actions jobs that execute the declared native checker. Each receipt binds the exact source SHA-256, tool version, command, return code, workflow run URL, commit SHA, runner OS, placeholder scan and log digest.

A failed job still uploads a typed `FAIL` receipt. No receipt in this directory grants governance authority or production-truth effect by itself.
