# KC144 P38 Meta Navigator V2

## Result

P38 changes the unit of progress from a serial “next step” to a complete,
dependency-aware crystal wave. A query is compiled once, projected through
seven crystal lenses, expanded through every lawful coordinate transform,
reduced in deterministic order, and returned through M12.

This is structural completion, not an assertion that unavailable empirical
evidence exists. The reference state is `CANDIDATE_HOLD`.

## Reconciled lineage

Two exact facts coexist:

- the public `demeet2k/guild-hall` branch is rooted at
  `KC144.P36.CANDIDATE::2dc88c9f2bf39ccb97e883f2`; and
- the recovered P37 source result
  `KC144.P37::039d3622874ac1ef067ce4da` names a different P36 parent,
  `KC144.P36::f40dc654ea7fc32651f1ebd6`.

P38 does not rewrite either fact. The first remains the public parent. The
second enters as an immutable sibling capsule bound by result, archive,
implementation, manifest, seal, and Meta Atlas commitments. P38 is the new
public child; P37 is a typed source input, never a silently reparented commit.

## Whole-crystal execution

The seven P38 lanes are:

| Lane | Function | Parallel group |
|---|---|---:|
| L01 | lineage reconciliation | 1 |
| L02 | exact P35 registry verification | 1 |
| L03 | dynamic multi-crystal query | 1 |
| L04 | live source routing | 1 |
| L05 | second one-edge experiment | 2 |
| L06 | held-out outcome calibration | 2 |
| L07 | independent IC10 return | 3 |

Groups 1 and 2 exploit independence. L07 is sequential because an IC10 return
must sign the exact candidate root produced by the prior lanes. The reducer is
fixed by lane number, so scheduler timing cannot change the result.

## Simultaneous coordinate field

The coordinate tensor covers GID001 through GID144. Each coordinate exposes:

- its 12×12 D4 address orbit;
- its native band action;
- its band invariant;
- its typed return obligation; and
- for KC27, both KC54 duplex shadows.

Native actions remain distinct:

- X16: `C4×C4` schedule and `V4×C4` algebra views;
- BR21: `C7×C3` rotations plus the `C2` mirror;
- F37: the P37 rail reflection;
- IC10: ordered conjunctive gates;
- KC15: the `S4` support action;
- KC27: the 48-element `B3` signed-permutation action;
- KC54: plus and conjugate shadows, never identity collapse;
- SSN12: solid-state observation and return indexing.

The reference tensor contains 144 coordinates, 1,056 distinct per-coordinate
D4 address views, 1,112 native orbit memberships, and 27 KC54 duplex nodes.
Every transform preserves source identity and has `truth_effect: NONE`.

## Exact registry boundary

P38 verifies the exact bytes of:

- 360 action crosswalk rows;
- 144 GID subscriptions; and
- 37 carrier subscriptions.

Verification checks file SHA-256 identities, line counts, unique typed row
identities, truth neutrality, zero observed production matches, and the
subscribed/waiting state. The public binding exports file and Merkle-style row
commitments, not Google Doc identifiers, revision identifiers, issuer
identities, titles, URLs, or source bodies.

Expected hashes without local byte verification remain
`EXPECTED_EXACT_BYTES`; they do not masquerade as `EXACT_BYTES_VERIFIED`.

## Source routing

P38 has three disjoint source classes:

- `GOOGLE_DOC_REVISION`;
- `REPOSITORY_BYTES`; and
- `LOCAL_ARTIFACT_BYTES`.

A Git source event binds repository, branch, commit, tree, path, blob SHA-1,
file SHA-256, and byte size. A Doc source event binds body, revision, and
issuer commitments. Neither shape can be accepted as the other. Admission is
read-only, consent-scoped, cutoff-bounded, content-addressed, and
non-promoting.

## Second one-edge experiment

P37’s first proposal edge was:

`GID047/F04 <-> GID064/F21`

P37 then re-differentiated the remaining candidates. Its unique rank-one
successor is:

`GID135/M03 <-> GID047/F04`

P38 applies that edge only to a copy of the P37 proposal graph and only after
lineage, exact registry, and repository-byte prerequisites pass. The
source-bound measurement is:

| Metric | Before | After | Reduction |
|---|---:|---:|---:|
| endpoint distance | 6 | 1 | 5 |
| router distance sum | 70 | 55 | 15 |
| global distance sum | 50,376 | 49,326 | 1,050 |
| global diameter | 11 | 10 | 1 |

The copied proposal graph reaches 312 edges. The canonical Meta Atlas remains
at 310 edges and its SHA-256 identity remains unchanged. Structural
improvement is not a real-world outcome.

## Outcome and authority membranes

Calibration accepts only consented `TASK_OUTCOME` or `EMPIRICAL_RESULT`
packets in the `HELD_OUT` partition from production, user-observed, or
connector-observed origins. Route-generated, test, synthetic, future,
duplicate, unverified, or non-consented rows fail closed.

Calibration requires at least 12 accepted outcomes across at least three
source surfaces and three routes. Even then, P38 emits proposed weight updates
only. Canonical weight updates remain zero.

Signer enrollment requires Ed25519 proof of possession, an exact P38 scope,
an external-independence declaration, and a validity interval. Enrollment
grants no authority. A successor can become `READY` only when an enrolled,
unrevoked, in-window independent signer returns:

- the exact P38 candidate root;
- the exact source-routing digest;
- the exact ready calibration digest;
- ten ordered IC10 PASS gates;
- a unique nonce; and
- a domain-separated Ed25519 signature.

Without that return, authority remains `NONE` and the global state remains
`HOLD`.

## Current honest state

- exact P35 registry bytes: available and verifiable;
- public repository-byte event: generated only after the implementation
  commit exists;
- second proposal edge: executable after those prerequisites;
- canonical graph mutation: zero;
- held-out outcomes: zero;
- canonical weight updates: zero;
- enrolled independent signers: zero;
- independent IC10 returns: zero;
- production mutation: false;
- truth effect: none;
- production authority: hold.

The successor frontier is:

`KC144.V4.0::MATH144.P39::LIVE_OUTCOME_CORPUS_INDEPENDENT_IC10_CONVERGENCE_WEIGHT_CALIBRATION_AND_CANONICAL_SUCCESSOR_DECISION_MACROCYCLE_08`
